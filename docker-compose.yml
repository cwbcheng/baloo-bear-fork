services:
  db:
    image: postgres:17-alpine
    container_name: baloo-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-baloo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: ${POSTGRES_DB:-baloo}
    volumes:
      - baloo_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-baloo}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  baloo:
    build:
      context: .
      dockerfile: Dockerfile
    image: baloo:local
    container_name: baloo-agent
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - ${BALOO_ENV_FILE:-.env}
    volumes:
      - ./.secrets:/app/.secrets:ro
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      MAX_CONCURRENT_REVIEWS: ${MAX_CONCURRENT_REVIEWS:-3}
      REVIEW_STALE_TIMEOUT_MINUTES: ${REVIEW_STALE_TIMEOUT_MINUTES:-30}
      WEBHOOK_DELIVERY_DEDUPE_TTL_SECONDS: ${WEBHOOK_DELIVERY_DEDUPE_TTL_SECONDS:-900}
      AGENT_MODEL: ${AGENT_MODEL:-claude-sonnet-4-6}
      AGENT_MAX_TOKENS: ${AGENT_MAX_TOKENS:-4096}
      AGENT_TEMPERATURE: ${AGENT_TEMPERATURE:-0.2}
      AGENT_MAX_TURNS: ${AGENT_MAX_TURNS:-30}
      OPENCODE_API_KEY: ${OPENCODE_API_KEY:-}
      REVIEW_AUTO_APPROVE: ${REVIEW_AUTO_APPROVE:-false}
      REVIEW_MIN_SEVERITY: ${REVIEW_MIN_SEVERITY:-MEDIUM}
      REVIEW_POST_ALL_SEVERITIES: ${REVIEW_POST_ALL_SEVERITIES:-false}
      REVIEW_USE_REQUEST_CHANGES: ${REVIEW_USE_REQUEST_CHANGES:-false}
      DATABASE_ENABLED: ${DATABASE_ENABLED:-false}
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://${POSTGRES_USER:-baloo}:${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}@db:5432/${POSTGRES_DB:-baloo}}
      INSTALLATION_ID: ${INSTALLATION_ID:-}
      DASHBOARD_ENABLED: ${DASHBOARD_ENABLED:-true}
      DASHBOARD_USERNAME: ${DASHBOARD_USERNAME:-}
      DASHBOARD_PASSWORD: ${DASHBOARD_PASSWORD:-}
      LINEAR_API_KEY: ${LINEAR_API_KEY:-}
      LINEAR_API_URL: ${LINEAR_API_URL:-https://api.linear.app/graphql}
      REPO_CACHE_ENABLED: ${REPO_CACHE_ENABLED:-true}
      REPO_CACHE_ROOT: ${REPO_CACHE_ROOT:-/tmp/baloo-repo-cache}
      REPO_CACHE_MAX_DISK_GB: ${REPO_CACHE_MAX_DISK_GB:-10}
      REPO_SANDBOX_MODE: ${REPO_SANDBOX_MODE:-bwrap}
      DOCUMENTATION_DRIFT_ENABLED: ${DOCUMENTATION_DRIFT_ENABLED:-false}
      DOCUMENTATION_DRIFT_CATALOG_PATH: ${DOCUMENTATION_DRIFT_CATALOG_PATH:-.baloo/documentation-catalog.json}
      DOCUMENTATION_DRIFT_MODEL: ${DOCUMENTATION_DRIFT_MODEL:-sonnet}
    ports:
      - "127.0.0.1:8000:8000"

volumes:
  baloo_pgdata:
